import {
    Menu
} from "react-admin";

const MenuCustom = (props: any) => {

    return (
        <Menu
            {...props}
        >
            <div>menu</div>
        </Menu>
    )
}

export default MenuCustom;